# sharp (Next image optimisation), unrs-resolver, and core-js (pulled in by
# butterchurn-presets) ship native/postinstall build scripts. We don't need them
# built, so ignore them explicitly — keeps `pnpm install` non-interactive/exit-0.
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
  - core-js
